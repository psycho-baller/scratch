import { defineConfig } from "@trigger.dev/sdk/v3";

// You can find your project ref in the Project Settings page of the Trigger.dev dashboard
export default defineConfig({
  project: "proj_twyueyslrkszqzqvwzsk",
  dirs: ["./trigger"],
  maxDuration: 300,
});